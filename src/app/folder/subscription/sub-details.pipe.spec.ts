import { SubDetailsPipe } from './sub-details.pipe';

describe('SubDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new SubDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
